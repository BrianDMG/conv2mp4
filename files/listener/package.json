{
  "description": "conv2mp4: Convert video files to a universal format to minimize transcoding",
  "scripts": {
    "install": "yarn install --force --ignore-scripts --modules-folder pode_modules",
    "start": "daemon.ps1",
    "test": "invoke-pester ./tests/*.ps1",
    "build": "psake"
  },
  "version": "5.0.0",
  "name": "conv2mp4",
  "author": "BrianDMG",
  "main": "daemon.ps1",
  "license": "MIT"
}
